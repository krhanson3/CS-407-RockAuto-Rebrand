export type Vehicle = {
    make: string;
    model: string;
    year: string;
    engine: string;
};

export type Address = { 
    name: string; 
    line1: string; 
    line2: string 
};

export type Part = {
    name: string;
    brand: string;
    partNumber: number;
    price: string;
    image: string; 
}
